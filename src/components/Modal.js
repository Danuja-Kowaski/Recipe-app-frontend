import React from 'react'
import Model from 'react-bootstrap/Modal'

const Modal = ({item, setOpen, deleteRecipe}) => {
  return (
    <div>
        Hello
    </div>
  )
//   <button type="button" className="btn btn-primary" onClick={ () =>deleteRecipe(item._id)}>Delete</button>
//               <button type="button" className="btn btn-secondary" data-dismiss="modal" onClick={() => setOpen({item: null, state: false})}>Close</button>
}

export default Modal